# 增量部署说明（2026-04-01）

本文档用于本次“生产工序二期（真实测算）”增量发布，重点覆盖：
- 数据库增量脚本执行顺序
- 代码变更点（模型/服务/页面）
- OpenClaw 自动部署建议步骤
- 上线后验收与回滚建议

---

## 0. 重要约束（先看）

- `scripts/sql/run_*.sql` 只允许**新增**，不改历史文件。
- **`run_40` 作为一期脚本保留**（不覆盖、不改语义）。
- 本次二期增量脚本使用：`scripts/sql/run_41_production_phase2_real_measurement.sql`。
- 全量文件 `scripts/sql/00_full_schema.sql` 已同步新表定义；**生产增量发布不要直接执行全量脚本**。

---

## 1. 本次修改点（按模块）

## 1.1 数据库结构（生产二期）

新增（以 `run_41` 为准）：
- `production_process_node`（工序节点：支持层级）
- `production_process_edge`（工序依赖边：DAG）
- `production_routing_node_override`（产品级节点覆写）
- `production_work_order_operation_plan`（工序排程结果）
- `production_material_plan_detail`（材料测算分摊）
- `production_cost_plan_detail`（成本分项）

同步更新：
- `scripts/sql/00_full_schema.sql`

## 1.2 ORM 模型

新增模型文件：
- `app/models/production_process_node.py`
- `app/models/production_process_edge.py`
- `app/models/production_routing_node_override.py`
- `app/models/production_work_order_operation_plan.py`
- `app/models/production_material_plan_detail.py`
- `app/models/production_cost_plan_detail.py`

模型注册更新：
- `app/models/__init__.py`

## 1.3 业务服务

新增：
- `app/services/production_schedule_svc.py`
- `app/services/production_cost_svc.py`

改造：
- `app/services/production_svc.py`
  - 测算流程升级为多阶段（材料 -> 排程 -> 成本）
  - 重算时清理二期结果表并重建

## 1.4 工序模板与页面

改造：
- `app/main/routes_production.py`
  - 保存工序模板时支持“依赖关系（DAG）”解析与校验
  - 依赖合法性校验：step 范围、去重、自环、环检测
  - 不填依赖时自动按 `step_no` 生成串行 FS 边
- `app/templates/production/process_template_form.html`
  - 新增“工序依赖（DAG）”编辑区（前置/后置/类型/lag/备注）
- `app/templates/production/preplan_detail.html`
  - 新增排程总工时、测算总成本展示卡片

## 1.5 文档

新增：
- `docs/production-measurement-benchmark.md`（二期验收基线）

---

## 2. SQL 执行顺序（增量发布）

数据库：`sydixon_order`

按顺序执行：
1. `scripts/sql/run_39_company_default_is_default.sql`（若环境尚未执行）
2. `scripts/sql/run_40_production_process_dag_and_plan.sql`（一期脚本，按你的历史版本执行；若已执行则跳过）
3. `scripts/sql/run_41_production_phase2_real_measurement.sql`（二期真实测算）

说明：
- 若 `run_40` 与本次二期语义冲突，以你已发布的一期版本为准，二期以 `run_41` 补充。
- 失败即停，不要继续后续脚本。

---

## 3. OpenClaw 自动部署建议

## 3.1 部署前
- 备份 `sydixon_order`（至少备份生产相关表与 RBAC 表）。
- 确认本次代码已包含第 1 节列出的新增/改造文件。
- 确认应用环境变量与数据库连接正常。

## 3.2 执行 SQL
- 使用 OpenClaw 按第 2 节顺序执行。
- 每步记录执行结果（成功/失败、影响行数、报错信息）。

## 3.3 发布代码并重启
- 同步代码到目标机。
- 重启 Web 进程（确保菜单/能力/路由缓存刷新）。

## 3.4 最小验收
- 工序模板页可录入依赖行并保存成功。
- 预生产计划“重新测算”可执行且不报错。
- 详情页可看到：
  - 预计总工时
  - 排程总工时
  - 测算总成本

---

## 4. 验收 SQL（可选）

```sql
-- 二期新表存在性
SHOW TABLES LIKE 'production_process_node';
SHOW TABLES LIKE 'production_process_edge';
SHOW TABLES LIKE 'production_work_order_operation_plan';
SHOW TABLES LIKE 'production_material_plan_detail';
SHOW TABLES LIKE 'production_cost_plan_detail';

-- 抽查是否有测算落数（替换 preplan_id）
SELECT COUNT(*) FROM production_work_order_operation_plan WHERE preplan_id = 1;
SELECT COUNT(*) FROM production_material_plan_detail WHERE preplan_id = 1;
SELECT COUNT(*) FROM production_cost_plan_detail WHERE preplan_id = 1;
```

---

## 5. 回滚建议

- 代码回滚：回退到上个稳定版本并重启服务。
- 数据回滚：优先做“业务停写 + 数据修复”，不建议直接删表。
- 如仅需屏蔽二期能力，可先停用相关入口（模板依赖编辑、二期展示卡片）并保留表结构。

---

文档日期：2026-04-01

